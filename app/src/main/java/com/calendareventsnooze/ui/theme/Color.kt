package com.calendareventsnooze.ui.theme

import androidx.compose.ui.graphics.Color

// ---------------------------------------------------------------------------
// App (settings) palette — mellow Google-ish scheme (UI.1)
// ---------------------------------------------------------------------------
val AppBackground     = Color(0xFFF1F3F4) // light grey page background
val AppSurface        = Color(0xFFFFFFFF) // cards
val AppTopBar         = Color(0xFF5F6368) // darker grey tab bar
val AppTopBarText     = Color(0xFFFFFFFF)
val AppButtonRegular  = Color(0xFFE8F5E9) // light green — regular buttons
val AppButtonRegularText = Color(0xFF1B5E20)
val AppForceStop      = Color(0xFFC6FF00) // accent green — Force Stop
val AppForceStopText  = Color(0xFF1A1A1A)
val AppAccentBlue     = Color(0xFF4285F4) // accent blue — accentuated buttons
val AppAccentBlueText = Color(0xFFFFFFFF)
val AppGreenCheck     = Color(0xFF34A853) // permissions all-granted checkmark
val AppWarningYellow  = Color(0xFFF9AB00) // permissions pending
val AppDanger         = Color(0xFFC0392B)
val AppTextPrimary    = Color(0xFF202124)
val AppTextSecondary  = Color(0xFF5F6368)

// ---------------------------------------------------------------------------
// Alarm takeover palette (UI.2)
// ---------------------------------------------------------------------------
val AlarmBackground    = Color(0xFF202124) // UI.2 — dark neutral takeover background
val AlarmSnoozeButton  = Color(0xFFF1F3F4) // UI.2 — snooze presets (black text)
val AlarmSpecifyTime   = Color(0xFF5F6368) // UI.2 — Specify Time
val AlarmDateAndTime   = Color(0xFF5F6368) // UI.3 — Date & Time (was #E8F5E9 in UI.2)
val AlarmOpenCalendar  = Color(0xFFFF8C8A) // UI.3 — Open Calendar Event (dismisses the alarm)
val AlarmSurface       = Color(0xFF16213E)
val AlarmAccentGold    = Color(0xFFFFD700)
val AlarmDanger        = Color(0xFFC0392B)
val AlarmCalendarBlue  = Color(0xFF4285F4) // aligned to app accent blue
val AlarmTextPrimary   = Color(0xFFFFFFFF)
val AlarmTextSecondary = Color(0xFFB0B0CC)
val AlarmTextMuted     = Color(0xFF7070A0)
