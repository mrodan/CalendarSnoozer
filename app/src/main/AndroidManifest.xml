<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!--
      The service also declares specialUse: in the SILENT / vibrate-only profiles
      it plays no media at all, so mediaPlayback alone is not an honest
      declaration and newer releases enforce the match.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Round 20 — lets the optional "Unrestricted background battery usage" row
      open the system's own confirm dialog rather than dumping the user in a
      list of every installed app. Google Play restricts this permission to apps
      whose core function needs it; an alarm app qualifies, and a sideloaded
      build is unaffected either way.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
      B.5 — Android 11+ (API 30) package visibility. Without this block,
      getLaunchIntentForPackage()/resolveActivity() silently return null for the
      calendar apps, which made "Open Calendar Event" fall through to a bare
      ACTION_VIEW and show an unrelated app chooser ("Open with Google").
    -->
    <queries>
        <!--
          Round 20 — must stay in step with CalendarApps.KNOWN. A package that is
          not listed here is invisible to getLaunchIntentForPackage, so it can
          neither be offered in the Settings picker nor opened for an event.
        -->
        <package android:name="com.google.android.calendar" />
        <package android:name="com.android.calendar" />
        <package android:name="com.samsung.android.calendar" />
        <package android:name="com.miui.calendar" />
        <package android:name="com.xiaomi.calendar" />
        <package android:name="com.huawei.calendar" />
        <package android:name="com.coloros.calendar" />
        <package android:name="com.oplus.calendar" />
        <package android:name="com.oneplus.calendar" />
        <package android:name="com.vivo.calendar" />
        <package android:name="com.bbk.calendar" />
        <package android:name="com.transsion.calendar" />
        <package android:name="com.asus.calendar" />
        <package android:name="com.lge.calendar" />
        <package android:name="com.sonyericsson.organizer" />
        <package android:name="com.motorola.calendar" />
        <package android:name="com.microsoft.office.outlook" />
        <package android:name="com.google.android.gm" />
        <package android:name="com.fsck.k9" />
        <package android:name="com.anydo" />
        <package android:name="com.appgenix.bizcal" />
        <package android:name="com.underwood.calendar_beta" />
        <package android:name="ws.xsoh.etar" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="content" android:host="com.android.calendar" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_CALENDAR" />
        </intent>
    </queries>

    <application
        android:name=".CalendarEventSnoozeApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.CalendarEventSnooze">

        <!-- Main settings activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Full-screen alarm activity — completely separate from MainActivity.

          B.7 — the takeover must never be what the user comes back to. It is
          singleInstance, so it owns its own task, and that task used to survive
          in recents after the alarm was resolved: re-entering the app from the
          overview replayed the task's base intent and the takeover reopened for
          an alarm that no longer existed. excludeFromRecents keeps it out of the
          overview entirely and autoRemoveFromRecents drops the task once the
          last activity finishes, so "CalendarSnoozer" in recents can only ever
          be MainActivity.
        -->
        <activity
            android:name=".ui.AlarmActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:autoRemoveFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.CalendarEventSnooze.Alarm"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- Notification listener -->
        <service
            android:name=".service.CalendarNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Alarm foreground service -->
        <service
            android:name=".service.AlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Full-screen alarm takeover for calendar events; rings
                    until the user snoozes or dismisses it." />
        </service>

        <!-- Snooze alarm receiver -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.calendareventsnooze.TRIGGER_ALARM" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
